import NavigationList from './NavigationList';

function Header() {
  return (
    <header className="App-header">
      <nav>
        <NavigationList />
      </nav>
    </header>
  );
}

export default Header;